<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wipd.schulprojekt_mathe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_monitor"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".statistikClasses.StatistikRechnerActivity"
            android:parentActivityName=".statistikClasses.StatistikPageActivity"/>
        <activity android:name=".statistikClasses.StatistikPageActivity"
            android:parentActivityName=".StartPageActivity"/>
        <activity android:name=".prozesseClasses.ProzesseRechnerActivity"
            android:parentActivityName=".prozesseClasses.ProzessePageActivity"/>
        <activity android:name=".prozesseClasses.ProzessePageActivity"
            android:parentActivityName=".StartPageActivity"/>
        <activity android:name=".matrixClasses.MatrixPageActivity"
            android:parentActivityName=".StartPageActivity"/>
        <activity android:name=".StartPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>